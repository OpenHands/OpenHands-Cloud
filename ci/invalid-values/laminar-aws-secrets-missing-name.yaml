# Invalid by Laminar's fail guard: an enabled provider needs a secret identity.
laminar:
  enabled: true
  secrets:
    awsSecretsManager:
      enabled: true
      clusterName: ""
      secretName: ""
