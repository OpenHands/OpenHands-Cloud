# Vendored dependency archives (regenerated by `helm dependency update`) are
# ignored via the root .gitignore's `charts/**/charts/*.tgz` rule. The embedded
# subchart sources under charts/ are deliberately tracked, so don't blanket-ignore them.
my-values.yaml
