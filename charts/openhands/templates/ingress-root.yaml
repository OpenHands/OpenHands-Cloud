{{- if and .Values.enabled .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: openhands-root-ingress
  {{- $annotations := .Values.ingress.root.annotations | default .Values.ingress.annotations }}
  {{- $middlewareAnnotation := include "openhands.pathMiddlewareAnnotation" . }}
  {{- if or $annotations $middlewareAnnotation }}
  annotations:
    {{- with $annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
    {{- if $middlewareAnnotation }}
    {{ $middlewareAnnotation | nindent 4 }}
    {{- end }}
  {{- end }}
spec:
  ingressClassName: {{ .Values.ingress.class }}
  {{- if .Values.tls.enabled }}
  tls:
  - hosts:
    - {{ include "openhands.ingressHost" . }}
    secretName: app-all-hands-{{ .Values.tls.env }}-tls
  {{- end }}
  rules:
  - host: {{ include "openhands.ingressHost" . }}
    http:
      paths:
      - path: {{ include "openhands.routedPath" (dict "path" "/" "Values" .Values) }}
        pathType: Prefix
        backend:
          service:
            name: openhands-service
            port:
              number: 3000
{{- end }}