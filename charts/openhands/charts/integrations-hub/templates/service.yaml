apiVersion: v1
kind: Service
metadata:
  name: integrations-hub
  labels:
    app: integrations-hub
spec:
  ports:
  - port: 80
    targetPort: {{ .Values.service.port | default 8000 }}
    protocol: TCP
    name: http
  selector:
    app: integrations-hub
