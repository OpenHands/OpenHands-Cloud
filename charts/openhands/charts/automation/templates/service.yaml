apiVersion: v1
kind: Service
metadata:
  name: automation
  labels:
    {{- include "automation.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
    app: automation
spec:
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: automation
